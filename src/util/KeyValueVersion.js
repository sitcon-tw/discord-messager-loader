//@ts-check

export default class {

    /**
     * KeyValueVersion
     * @param {string} versionName 
     * @param {number} offset 
     */
    constructor(versionName, offset) {
        this.versionName = versionName;
        this.offset = offset;
    }
}